{
  "database": {
    "rules": "database.rules.json"
  },
  "firestore": {
    "rules": "rules.file",
    "indexes": "indexes.file"
  },
  "functions": {
    "predeploy": "npm --prefix \"$RESOURCE_DIR\" run build"
  }
}
